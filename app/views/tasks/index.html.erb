<h1>タスク 一覧</h1>
<p><%= link_to "投稿画面へ", new_task_path %></p>
<% @tasks.each do |task| %>
  <p>
    <%= task.title %>
    <%= link_to "詳細", task_path(task) %>
    <%= link_to "編集", edit_task_path(task) %>
    <%= link_to "削除", task_path(task), method: :delete %>
  </p>
<% end %>